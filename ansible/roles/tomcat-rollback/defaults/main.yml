---
# defaults/main.yml — tomcat-rollback role
tomcat_home:    /opt/tomcat
tomcat_webapps: "{{ tomcat_home }}/webapps"
tomcat_service: tomcat
app_name:       webapp
backup_dir:     /opt/tomcat/war-backups
